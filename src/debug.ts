/**
 * Debug mode: the debug panel, the map builder and the `window.session` handle.
 * Off by default. Turn it on locally with `VITE_DEBUG=true` in `.env.local` (see `.env.example`).
 */
export const DEBUG = import.meta.env.VITE_DEBUG === 'true';
